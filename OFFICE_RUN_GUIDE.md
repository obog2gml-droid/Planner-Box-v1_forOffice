# Planner Box for Office 실행 가이드

1. `Start Planner Box.command`를 더블클릭하면 서버가 실행되고 브라우저가 자동으로 열립니다.
2. 처음 실행 시에는 의존성 설치(`npm install`) 때문에 시간이 조금 걸릴 수 있습니다.
3. 종료할 때는 `Stop Planner Box.command`를 더블클릭하세요.

기본 접속 주소는 `http://localhost:3310` 입니다.
